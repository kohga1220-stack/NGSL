[project]
name = "ngsl"
version = "0.1.0"
description = "Next-Generation Statistical Language tooling"
authors = [
    { name = "GitHub Copilot", email = "noreply@github.com" }
]
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
